# Python
__pycache__/
*.pyc
.venv/

# Training checkpoints (large binaries, not in git)
checkpoints/
**/checkpoints/

# Kaggle data (large CSVs, not in git)
data/train.csv
data/test.csv
submission.zip

# WandB (cache, not in git)
wandb/

# IDE
.idea/
.vscode/

# Note: training_logs/ IS tracked — permanent record of every attempt.
# Note: data/*.jsonl IS tracked (small datasets for reproduction).
# Exception: balanced_train*.jsonl is too large (~93MB) for git
data/balanced_train.jsonl
data/balanced_train_meta.jsonl
*.sif
build-error.log
build-loop.sh
outputs/

# Auto-generated / artifacts (not source)
unsloth_compiled_cache/
*.bak
nvidia-nemotron-model-reasoning-challenge.zip
